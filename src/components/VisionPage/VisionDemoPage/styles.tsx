import { spacer24, spacer64 } from "@/styles/sizes";
import { styled } from "styled-components";

export const StyledHeaderSection = styled.section`
	display: flex;
	gap: ${spacer64};
	margin-bottom: ${spacer24};
`;

export const StyledTextBody = styled.div`
	max-width: 829px;
`;
